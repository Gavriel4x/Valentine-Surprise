<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>For <%= targetName %> 💖</title>
  <link rel="icon" type="image/png" href="/Icon.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/result.css">
</head>

<body class="result-page d-flex align-items-center justify-content-center">
  <div class="card shadow-lg p-4 text-center result-card">

    <h5 class="text-muted mb-2">Hai <%= targetName %> 🤍</h5>

    <h2 class="mb-3">
      <%= senderName %> punya sesuatu buat kamu
    </h2>

    <% if (message) { %>
      <p class="message-box">
        "<%= message %>"
      </p>
    <% } %>

    <div class="answers mt-4">
      <p class="fw-semibold mb-2">Dari jawaban kamu tadi...</p>

      <ul class="list-unstyled">
        <% answers.forEach(a => { %>
          <li>✨ <%= a %></li>
        <% }) %>
      </ul>
    </div>

    <div class="final-question mt-4">
      <h4>Jadi…</h4>
      <h3 class="fw-bold text-danger">
        Will you be my Valentine? 💘
      </h3>
    </div>

    <div class="d-flex gap-3 justify-content-center mt-4">
        <a
        href="https://wa.me/?text=<%= encodeURIComponent(
            'Aku jawab YES 💖\n\n' + shareUrl
        ) %>"
        class="btn btn-danger px-4"
        onclick="fireConfetti()"
        >
        YES 💖
        </a>


      <button class="btn btn-outline-secondary px-4" onclick="alert('Jawaban kamu sudah cukup kok 🤍')">
        Aku senyum aja 🙂
      </button>
    </div>

    <p class="mt-4 text-muted small">
      dibuat khusus oleh <%= senderName %>
    </p>
    <div class="text-center text-muted small mt-4" style="color:black !important">copyright @gavrielrey</div>
  </div>
</body>
</html>

<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<script>
  setTimeout(() => {
    fireConfetti();
  }, 600);

  function fireConfetti() {
    confetti({
      particleCount: 150,
      spread: 90,
      origin: { y: 0.6 },
      colors: ['#ff4d6d', '#ffb3c1', '#ffffff'],
      scalar: 1.1
    });
  }
</script>
