<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Untuk Kamu 💖</title>
  <link rel="icon" type="image/png" href="/Icon.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/choice.css">
</head>

<body class="valentine-bg">
  <div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="valentine-card text-center shadow-lg">

      <!-- progress -->
      <div class="progress mb-3" style="height: 6px;">
        <div
          class="progress-bar bg-danger"
          style="width: <%= (step / totalSteps) * 100 %>%">
        </div>
      </div>

      <div class="text-muted small mb-2">
        Pertanyaan <%= step %> dari <%= totalSteps %>
      </div>

      <h5 class="mb-4">
        <%= question %>
      </h5>

      <% options.forEach(opt => { %>
        <form method="POST" action="/v/<%= id %>/answer" class="mb-3">
          <input type="hidden" name="step" value="<%= step %>">
          <input type="hidden" name="answer" value="<%= opt %>">
          <button class="btn choice-btn w-100">
            <%= opt %>
          </button>
        </form>
      <% }) %>

      <div class="text-center text-muted small mt-4" style="color:black !important">copyright @gavrielrey</div>
    </div>
  </div>
</body>
</html>
